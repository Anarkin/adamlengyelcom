# adamlengyelcom

Aggregator site @ https://adamlengyel.com

Plain static site (HTML/CSS/JS) in `src/` — no framework, no build step.

- Deploys automatically to Azure Static Web Apps via GitHub Actions on push to `master`.
- `npm run format` — format everything with Prettier (`npm run format:check` to verify only).
